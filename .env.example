SENDER_API_TOKEN=
# From-address for the Friday digest campaigns. UNSET = digest dark-disabled
# (global kill switch). Must be on an authenticated sending domain — see
# docs/sender-setup.md §5.
SENDER_FROM_EMAIL=
SENDER_FROM_NAME=
SENDER_REPLY_TO=
SUBSCRIBER_TOKEN_SECRET=
SENTRY_DSN=
SENTRY_ENVIRONMENT=
