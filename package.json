{
  "name": "pitmaster-tools",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "browser:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
